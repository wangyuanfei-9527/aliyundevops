# Default is dry-run. Set to live only after local config is complete.
EXECUTION_MODE=dry-run

# Optional AI planner. Without this, the app uses a deterministic local planner.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.2

# Optional override for Yunxiao personal access token env name.
YUNXIAO_TOKEN=
